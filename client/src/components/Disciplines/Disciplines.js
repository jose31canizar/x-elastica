import React, { Component } from 'react'
import Section from '../Section/Section'

class Disciplines extends Component {
  render() {
    return (
      <Section name='Disciplines' data='disciplines'/>
    )
  }
}

export default Disciplines
